variables:
  GIT_SUBMODULE_STRATEGY: recursive

test:2.7:
  stage: test
  image: python:2.7
  script:
    - pip install enum34
    - make gensrc
    - python -m unittest discover

test:3.5:
  stage: test
  image: python:3.5
  script:
    - make gensrc
    - python -m unittest discover

test:3.8:
  stage: test
  image: python:3.8
  script:
    - make gensrc
    - python -m unittest discover
